
Процедура ТекстАвтоПодборТекста(Элемент, Текст, ТекстАвтоПодбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	Если Прав(Текст, 1) = "+" Тогда
		РедактируемыйТекст = СтрЗаменить(Текст, "+", "123456789");
	КонецЕсли;
	
	УстановитьКурсорВКонецТекста();
КонецПроцедуры

Процедура КоманднаяПанель1Ок(Кнопка)
	ВладелецФормы.ЭлементыФормы.Конструктор.ТекущиеДанные.Текст = РедактируемыйТекст;

	Закрыть();
КонецПроцедуры

Процедура ПриОткрытии()
	РедактируемыйТекст = ВладелецФормы.ЭлементыФормы.Конструктор.ТекущиеДанные.Текст;
	
	УстановитьКурсорВКонецТекста();
КонецПроцедуры

Процедура УстановитьКурсорВКонецТекста()
	РедактируемыйТекст = СокрЛП(РедактируемыйТекст) + " ";
	
	Край = СтрДлина(РедактируемыйТекст);
	
	ЭлементыФормы.РедактируемыйТекст.УстановитьГраницыВыделения(Край, Край+1);
КонецПроцедуры
